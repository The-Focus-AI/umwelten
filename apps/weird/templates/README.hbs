# {{projectName}}

This is a generated MCP server that ingests CSV into an in-memory SQLite database and exposes generic + domain tools.

## Quickstart

1. pnpm install
2. pnpm start

Set `GOOGLE_API_KEY` in your environment if you plan to use LLM-backed domain tools.