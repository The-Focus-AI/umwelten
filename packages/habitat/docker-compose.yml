services:
  habitat:
    build:
      context: ../..
      dockerfile: packages/habitat/Dockerfile
    ports:
      - "7430:8080"
    volumes:
      - habitat-data:/data
    env_file:
      - ../../.env
    environment:
      - PORT=8080

volumes:
  habitat-data:
